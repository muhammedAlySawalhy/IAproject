let UserModel;_1e9‍.w("../models/UserModel",[["default",["UserModel"],function(v){UserModel=v}]]);const express = require("express");


const AuthController = require("../controllers/AuthController");
const router = express.Router();

// Signup Route

router.post("/signup", async (req, res) => {
  const { username, email, password } = req.body;
  // Generate a unique ID for the user

  // Create a new user object with the given properties
  const newUser = new UserModel(username, email, password);
  // Insert the new user into the database
  const result = await AuthController.createUser(newUser);
  res.send(result);
});

// Login Route
router.post("/login", async (req, res) => {
  const { username, password } = req.body;
  // Find the user with the given username
  _1e9‍.g.console.log("req", password);
  const user = await AuthController.getUser(username);
  _1e9‍.g.console.log("user get", user.data);
  if (user.success) {
    // Check if the password matches
    if (user.data.password == password) {
      res.send(user);
    } else {
      res.status(401).send({ message: "Invalid password" });
    }
  } else {
    res.status(404).send({ message: "User not found" });
  }
});

module.exports = router;
